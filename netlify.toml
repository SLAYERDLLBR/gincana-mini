[build]
  command = "cd apps/web-online && npm install && npm run build"
  publish = "apps/web-online/.next"
  base = "."

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
